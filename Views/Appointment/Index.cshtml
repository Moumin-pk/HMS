@* @model IEnumerable<HMS.Data.Models.Appointment>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PatientId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DoctoreId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AppointmentDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Purpose)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsCompleted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PatientId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DoctoreId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AppointmentDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Purpose)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsCompleted)
            </td>
            <td>
                    <a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-primary" aria-label="Edit">
                        <i class="bi bi-pencil"></i>
                    </a> |
                    <a href="@Url.Action("Details", new { id = item.Id })" class="btn btn-info" aria-label="Details">
                        <i class="bi bi-eye"></i>
                    </a> |
                    <a href="@Url.Action("Delete", new { id = item.Id })" class="btn btn-danger" aria-label="Delete"
                       onclick="return confirm('Are you sure you want to delete this item?');">
                        <i class="bi bi-trash"></i>
                    </a>

            </td>
        </tr>
}
    </tbody>
</table>
 *@